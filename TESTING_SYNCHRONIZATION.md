# Тестирование синхронизации CodeLab IDE

## 🧪 Как протестировать синхронизацию

### **1. Открытие проекта**
- Запустите приложение
- Откройте любой проект через StartWizardPanel
- Убедитесь, что ExplorerPanel показывает файловое дерево

### **2. Открытие файлов**
- Кликните на файл в ExplorerPanel
- Убедитесь, что EditorPanel открывает файл
- Проверьте, что содержимое файла отображается правильно

### **3. Синхронизация открытия файлов**
- Откройте несколько файлов по очереди
- Убедитесь, что каждый файл открывается в EditorPanel
- Проверьте, что ExplorerPanel подсвечивает выбранный файл

### **4. Отслеживание внешних изменений**
- Откройте файл в CodeLab IDE
- Измените этот же файл во внешнем редакторе (VS Code, TextEdit и т.д.)
- Сохраните изменения во внешнем редакторе
- Убедитесь, что CodeLab IDE обнаруживает изменения

### **5. Создание новых файлов**
- Создайте новый файл в файловой системе
- Убедитесь, что ExplorerPanel автоматически обновляется
- Новый файл должен появиться в файловом дереве

### **6. Удаление файлов**
- Удалите файл из файловой системы
- Убедитесь, что ExplorerPanel убирает файл из дерева
- Если файл был открыт в EditorPanel, он должен закрыться

### **7. Переименование файлов**
- Переименуйте файл в файловой системе
- Убедитесь, что изменения отражаются в ExplorerPanel
- Проверьте, что EditorPanel обновляет название вкладки

## 🔧 Отладка проблем

### **Если синхронизация не работает:**

1. **Проверьте DI контейнер:**
   ```dart
   // Убедитесь, что FileSyncService зарегистрирован в AppDiModule
   bind<FileSyncService>().toProvide(() => FileSyncService(...)).singleton();
   ```

2. **Проверьте подписки:**
   - ExplorerPanel должен слушать fileDeletedStream и fileTreeChangedStream
   - EditorPanel должен слушать fileOpenedStream и fileChangedStream

3. **Проверьте FileWatcherService:**
   - Убедитесь, что watchDirectory() вызывается при открытии проекта
   - Проверьте, что события файловой системы обрабатываются

### **Логи для отладки:**

Добавьте логирование в ключевых местах:

```dart
// В FileSyncService._handleFileSystemEvent
print('FileSystemEvent: ${event.type} - ${event.path}');

// В ExplorerPanel._setupFileSyncListeners  
print('ExplorerPanel: Setting up file sync listeners');

// В EditorPanel._setupFileSyncListeners
print('EditorPanel: Setting up file sync listeners');
```

## 📊 Ожидаемое поведение

### **Успешная синхронизация:**
- ✅ Файлы открываются при клике в ExplorerPanel
- ✅ Внешние изменения обнаруживаются автоматически
- ✅ Файловое дерево обновляется при изменениях
- ✅ Удаленные файлы убираются из интерфейса

### **Индикаторы работы:**
- Иконка синхронизации в статус-баре (зеленая - работает)
- Автоматическое обновление интерфейса
- Отсутствие ошибок в консоли

## 🚀 Запуск тестов

```bash
# Запуск приложения
cd apps/codelab_ide
flutter run

# Проверка анализа кода
dart analyze

# Проверка зависимостей
dart pub get
```

---

*Система синхронизации готова к тестированию!!!!*
