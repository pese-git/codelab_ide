# AI Assistant UI Redesign

## Обзор изменений

Переработан UI панели AI Assistant в стиле RooCode. Теперь при открытии панели сначала отображается список сессий, а после выбора сессии или создания новой открывается чат с AI агентом.

## Архитектура

### Новые компоненты

#### 1. SessionListView (`lib/src/ui/session_list_view.dart`)
Виджет для отображения списка сессий с возможностью:
- Просмотра всех доступных сессий
- Создания новой сессии
- Переключения между сессиями
- Удаления сессий
- Отображения информации о каждой сессии (количество сообщений, текущий агент, время последней активности)

**Особенности дизайна:**
- Карточный стиль для каждой сессии
- Цветовая индикация текущего агента
- Визуальное выделение активной сессии
- Кнопка "New Session" в верхней части списка
- Empty state с призывом к действию

#### 2. ChatView (`lib/src/ui/chat_view.dart`)
Виджет для отображения чата с AI агентом:
- Список сообщений с разделением на пользовательские и ассистентские
- Поле ввода сообщений
- Кнопка возврата к списку сессий
- Селектор агентов
- Обработка tool approval запросов

**Особенности дизайна:**
- Bubble-стиль сообщений с аватарами
- Цветовая кодировка типов сообщений (user, assistant, tool, error)
- Markdown рендеринг для сообщений
- Автоскролл к последнему сообщению
- Empty state для нового чата

#### 3. AiAssistantPanel (переработан)
Главный виджет панели с навигацией между списком сессий и чатом:
- Управление состоянием навигации (показывать список или чат)
- Интеграция с SessionManagerBloc
- Автоматическое определение начального состояния (если есть история - показать чат)
- Плавные переходы между экранами (AnimatedSwitcher)

## Поток работы пользователя

### 1. Открытие панели AI Assistant
```
Пользователь открывает панель → Отображается SessionListView
```

### 2. Создание новой сессии
```
Нажатие "New Session" → SessionManagerBloc создает сессию → 
Переход к ChatView с пустой историей
```

### 3. Выбор существующей сессии
```
Клик на сессию → Загрузка истории через SessionManagerBloc → 
Переход к ChatView с загруженной историей
```

### 4. Возврат к списку сессий
```
Нажатие кнопки "Back" в ChatView → Переход к SessionListView → 
Обновление списка сессий
```

## Интеграция с существующими компонентами

### SessionManagerBloc
- Используется для управления сессиями
- События: `loadSessions`, `createNewSession`, `switchToSession`, `deleteSession`
- Состояния: `initial`, `loading`, `loaded`, `error`, `sessionSwitched`, `newSessionCreated`

### AiAgentBloc
- Управляет чатом и взаимодействием с AI
- Новое событие: `loadHistory` - для загрузки истории сессии
- Состояние `ChatState` содержит историю сообщений и текущего агента

### Модели данных
- `SessionInfo` - информация о сессии (ID, количество сообщений, последняя активность)
- `SessionHistory` - полная история сессии с сообщениями
- `WSMessage` - сообщение в чате (различные типы: user, assistant, tool, error)

## Стилистика (RooCode Style)

### Цветовая схема
- **User messages**: синий фон с синей рамкой
- **Assistant messages**: серый фон с серой рамкой
- **Tool calls**: оранжевый фон с оранжевой рамкой
- **Tool results**: зеленый фон с зеленой рамкой
- **Errors**: красный фон с красной рамкой
- **Agent switches**: фиолетовый фон с фиолетовой рамкой

### Агенты
- 🪃 **Orchestrator** - фиолетовый
- 💻 **Code** - синий
- 🏗️ **Architect** - оранжевый
- 🪲 **Debug** - красный
- ❓ **Ask** - зеленый

### UI элементы
- Карточки с закругленными углами (8px)
- Аватары для сообщений (32x32px, круглые)
- Иконки для типов сообщений
- Badges для статусов (Active, агенты)
- Плавные переходы между экранами

## Обратная совместимость

Изменения полностью обратно совместимы:
- Если `SessionManagerBloc` не зарегистрирован в DI, панель работает в режиме "только чат"
- Существующий код использования `AiAssistantPanel` не требует изменений
- Все существующие функции сохранены

## Файлы изменений

### Новые файлы
- `lib/src/ui/session_list_view.dart` - виджет списка сессий
- `lib/src/ui/chat_view.dart` - виджет чата
- `AI_ASSISTANT_UI_REDESIGN.md` - эта документация

### Измененные файлы
- `lib/src/ui/ai_assistant_panel.dart` - переработан для навигации между экранами

### Удаленные файлы
- Нет (старый функционал сохранен в новых компонентах)

## Использование

```dart
// В ide_root_page.dart или другом месте
AiAssistantPanel(bloc: aiAgentBloc)
```

Панель автоматически определит, показывать список сессий или чат, в зависимости от:
1. Наличия `SessionManagerBloc` в DI
2. Наличия истории в текущей сессии

## Будущие улучшения

1. **Поиск по сессиям** - добавить поле поиска в SessionListView
2. **Фильтрация сессий** - по агенту, дате, количеству сообщений
3. **Переименование сессий** - возможность задать имя сессии
4. **Экспорт истории** - сохранение истории сессии в файл
5. **Настройки отображения** - компактный/расширенный вид списка
6. **Группировка сессий** - по дате (сегодня, вчера, на этой неделе)
7. **Предпросмотр сообщений** - показывать первое сообщение в карточке сессии

## Тестирование

Для тестирования нового UI:

1. Запустите приложение
2. Откройте панель AI Assistant
3. Проверьте отображение списка сессий
4. Создайте новую сессию
5. Отправьте несколько сообщений
6. Вернитесь к списку сессий
7. Переключитесь на другую сессию
8. Проверьте удаление сессии

## Известные ограничения

1. Удаление сессий пока не реализовано на backend (требуется DELETE endpoint в Gateway)
2. Переименование сессий не поддерживается
3. Поиск и фильтрация сессий отсутствуют
