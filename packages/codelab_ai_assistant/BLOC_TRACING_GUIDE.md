# Руководство по трейсингу Bloc состояний

## Обзор

В проект добавлена система трейсинга для отслеживания состояний всех Bloc'ов в приложении. Это помогает в отладке и понимании потока данных.

## Компоненты

### 1. AppBlocObserver

Глобальный наблюдатель за всеми Bloc'ами в приложении, который логирует:

- **onCreate** - создание нового Bloc
- **onEvent** - получение события Bloc'ом
- **onChange** - изменение состояния
- **onTransition** - полный переход (событие → текущее состояние → новое состояние)
- **onError** - ошибки в Bloc'ах
- **onClose** - закрытие Bloc

Файл: [`lib/core/bloc/app_bloc_observer.dart`](lib/core/bloc/app_bloc_observer.dart)

### 2. Улучшенное логирование в Bloc'ах

Все Bloc'и теперь имеют детальное логирование с эмодзи для лучшей читаемости:

#### AgentChatBloc
- 📤 Отправка сообщений
- 📨 Получение сообщений
- 🔄 Переключение агентов
- 📜 Загрузка истории
- 🔌 Подключение/отключение
- 🔔 Запросы подтверждения
- ✅ Подтверждение действий
- ❌ Отклонение действий

#### AuthBloc
- 🔍 Проверка статуса авторизации
- 🔐 Вход в систему
- 🚪 Выход из системы
- 🔄 Обновление токена
- 🔓 Неавторизованное состояние

#### SessionManagerBloc
- 📋 Загрузка сессий
- ➕ Создание новой сессии
- 🔍 Выбор сессии
- 🗑️ Удаление сессии
- 🔄 Обновление списка сессий

#### ToolApprovalBloc
- 🔔 Запрос подтверждения
- ✅ Подтверждение выполнения
- ❌ Отклонение выполнения
- ✏️ Модификация параметров
- 🚫 Отмена операции

## Инициализация

### Способ 1: Автоматическая инициализация через модуль

BlocObserver автоматически регистрируется в DI контейнере:

```dart
import 'package:codelab_ai_assistant/codelab_ai_assistant.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // Создаем модуль
  final module = AiAssistantModule(
    gatewayBaseUrl: 'http://localhost:8000',
    // ... другие параметры
  );
  
  // Инициализируем DI
  final scope = await Cherrypick.init([module]);
  
  // Получаем logger и инициализируем BlocObserver
  final logger = scope.resolve<Logger>();
  initializeBlocObserver(logger);
  
  runApp(MyApp(scope: scope));
}
```

### Способ 2: Ручная инициализация

```dart
import 'package:codelab_ai_assistant/codelab_ai_assistant.dart';
import 'package:logger/logger.dart';

void main() {
  final logger = Logger();
  
  // Инициализируем BlocObserver
  initializeBlocObserver(logger);
  
  runApp(MyApp());
}
```

## Формат логов

### Создание Bloc
```
🔷 [BlocObserver] onCreate: AgentChatBloc
```

### Получение события
```
📥 [BlocObserver] onEvent: AgentChatBloc
  Event: SendMessageEvent(text: "Hello")
```

### Изменение состояния
```
🔄 [BlocObserver] onChange: AgentChatBloc
  Current: AgentChatState(messages: [], isLoading: false, ...)
  Next: AgentChatState(messages: [Message(...)], isLoading: true, ...)
```

### Полный переход
```
🔀 [BlocObserver] onTransition: AgentChatBloc
  Event: SendMessageEvent(text: "Hello")
  Current: AgentChatState(messages: [], isLoading: false, ...)
  Next: AgentChatState(messages: [Message(...)], isLoading: true, ...)
```

### Ошибка
```
❌ [BlocObserver] onError: AgentChatBloc
  Error: Connection failed
```

### Закрытие Bloc
```
🔶 [BlocObserver] onClose: AgentChatBloc
```

## Детальные логи в Bloc'ах

Каждый Bloc также имеет свои собственные детальные логи:

```
[AgentChatBloc] 📤 Sending message: "Hello, how are you?"
[AgentChatBloc] 📝 Adding user message to state, total messages: 1
[AgentChatBloc] ✅ Message sent successfully
[AgentChatBloc] 📨 Message received: assistant, content type: MessageContent
```

## Фильтрация логов

Для фильтрации логов по конкретному Bloc используйте поиск по префиксу:

- `[AgentChatBloc]` - логи чата с агентом
- `[AuthBloc]` - логи авторизации
- `[SessionManagerBloc]` - логи управления сессиями
- `[ToolApprovalBloc]` - логи подтверждения инструментов
- `[BlocObserver]` - общие логи наблюдателя

## Отключение трейсинга

Если нужно отключить трейсинг, просто не вызывайте `initializeBlocObserver()` или установите `null`:

```dart
import 'package:flutter_bloc/flutter_bloc.dart';

void main() {
  Bloc.observer = null; // Отключить трейсинг
  runApp(MyApp());
}
```

## Настройка уровня логирования

Измените уровень логирования в Logger для контроля детализации:

```dart
final logger = Logger(
  level: Level.debug, // trace, debug, info, warning, error, fatal
  printer: PrettyPrinter(
    methodCount: 0,
    errorMethodCount: 5,
    lineLength: 80,
    colors: true,
    printEmojis: true,
  ),
);
```

## Производительность

BlocObserver имеет минимальное влияние на производительность:
- Логи форматируются только при необходимости
- Длинные состояния автоматически обрезаются (до 200 символов)
- Используется эффективное логирование через Logger

## Отладка проблем

### Проблема: Логи не появляются

**Решение:**
1. Убедитесь, что `initializeBlocObserver()` вызвана до создания любых Bloc'ов
2. Проверьте уровень логирования Logger
3. Убедитесь, что Logger правильно настроен

### Проблема: Слишком много логов

**Решение:**
1. Увеличьте уровень логирования до `Level.info` или `Level.warning`
2. Используйте фильтрацию по префиксу в консоли
3. Отключите эмодзи в PrettyPrinter если они мешают

### Проблема: Состояния обрезаются

**Решение:**
Это нормальное поведение для больших состояний. Если нужно видеть полное состояние, измените лимит в `AppBlocObserver._formatState()`:

```dart
if (stateStr.length > 500) { // Увеличьте с 200 до 500
  return '${stateStr.substring(0, 500)}...';
}
```

## Примеры использования

### Отслеживание потока авторизации

```
[AuthBloc] 🔐 Logging in user: admin
🔀 [BlocObserver] onTransition: AuthBloc
  Event: Login(username: admin, password: ***)
  Current: Unauthenticated()
  Next: Authenticating()
[AuthBloc] ✅ Login successful
🔀 [BlocObserver] onTransition: AuthBloc
  Event: Login(username: admin, password: ***)
  Current: Authenticating()
  Next: Authenticated(token: AuthToken(...))
```

### Отслеживание создания сессии

```
[SessionManagerBloc] ➕ Creating new session...
🔀 [BlocObserver] onTransition: SessionManagerBloc
  Event: CreateSession()
  Current: Loaded(sessions: [])
  Next: Loading()
[SessionManagerBloc] ✅ Created session: abc-123
🔀 [BlocObserver] onTransition: SessionManagerBloc
  Event: CreateSession()
  Current: Loading()
  Next: NewSessionCreated(sessionId: abc-123)
```

## Заключение

Система трейсинга предоставляет полную видимость в работу всех Bloc'ов приложения, что значительно упрощает отладку и понимание потока данных.
