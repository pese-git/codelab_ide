# Руководство по синхронизации CodeLab IDE

## 🎯 Обзор архитектуры синхронизации

CodeLab IDE использует централизованную систему синхронизации между компонентами:

### **Основные компоненты:**

1. **FileSyncService** - центральный координатор
2. **FileWatcherService** - отслеживание файловой системы
3. **ExplorerPanel** - файловое дерево
4. **EditorPanel** - редактор файлов

## 🔄 Поток данных синхронизации

### **Открытие файла:**
```
ExplorerPanel → FileSyncService → EditorPanel
```

### **Сохранение файла:**
```
EditorPanel → FileSyncService → FileSystem → ExplorerPanel

### **Внешние изменения:**
```
FileSystem → FileWatcherService → FileSyncService → EditorPanel/ExplorerPanel

## 📋 Реализованные события синхронизации:

### 1. **FileOpened** - файл открыт в редакторе
### 2. **FileSaved** - файл сохранен
### **FileChanged** - файл изменен извне
### 3. **FileDeleted** - файл удален
### 4. **FileTreeChanged** - файловое дерево изменилось

## 🛠️ Использование:

### **В DI модуле:**
```dart
bind<FileSyncService>()
    .toProvide(() => FileSyncService(
          fileService: currentScope.resolve<FileService>(),
          fileWatcherService: currentScope.resolve<FileWatcherService>(),
          projectManagerService: currentScope.resolve<ProjectManagerService>(),
    ))
    .singleton();
```

### **В ExplorerPanel:**
```dart
ExplorerPanel(
  onFileOpen: (node, content) {
    // Файл открывается через FileSyncService
    // EditorPanel автоматически получит уведомление
```

## 📁 Структура файлов:

```
codelab_core/
├── services/
│   ├── file_service.dart
│   ├── file_watcher_service.dart
```

### **В EditorPanel:**
```dart
EditorPanel(
  // Автоматически получает уведомления об открытии файлов
```

## 🔧 Настройка:

1. **FileWatcherService** автоматически запускается при открытии проекта
2. **FileSyncService** слушает события от FileWatcherService
3. **ExplorerPanel** и **EditorPanel** подписываются на события

## 🎯 Преимущества:

- **Автоматическая синхронизация** между всеми компонентами
- **Отслеживание внешних изменений** (другие редакторы, git и т.д.)
- **Централизованное управление** событиями файловой системы
- **Расширяемость** - легко добавить новые компоненты
- **Надежность** - обработка ошибок и восстановление

## 🚀 Дальнейшее развитие:

1. **Конфликты изменений** - когда файл изменен и в IDE и извне
2. **Авто-сохранение** при внешних изменениях
3. **Уведомления пользователя** о внешних изменениях
```

## 📞 Отладка:

Для отладки синхронизации используйте:
- Логирование в консоль
- Индикатор синхронизации в статус-баре

---

*Система готова к использованию!*

!!!
